# Copy this file to /etc/default/household-webpage.
#
# Generate a value with:
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=bigfatkey456holdon123almosthere789

# Uncomment these only if password-reset email is needed.
# MAIL_USERNAME=example@gmail.com
# MAIL_PASSWORD=replace-with-an-app-password

# The application defaults to home_page/site.db. To use another database:
# SQLALCHEMY_DATABASE_URI=sqlite:////absolute/path/to/site.db
